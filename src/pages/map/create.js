import Typography from "@/components/theme/overrides/Typography";
import { Box } from "@mui/material";
export default function Create() {
  return (
    <Box>
      <Typography>Create New Map</Typography>
    </Box>
  );
}
